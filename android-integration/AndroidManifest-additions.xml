<!--
  Add these <uses-permission> lines as siblings of the existing ones,
  and the two entries inside <application> as siblings of the existing
  <activity> block, in android/app/src/main/AndroidManifest.xml
-->

<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<!-- inside <application> ... </application> -->

<service
    android:name="ai.myraa.mobile.plugins.DeviceControlAccessibilityService"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
    android:exported="false">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>

<!-- NEW: one-screen permissions hub, reachable via androidAction
     OPEN_PERMISSIONS_SCREEN. No theme given on purpose - it inherits
     the app's default theme instead of guessing a style name. -->
<activity
    android:name="ai.myraa.mobile.plugins.PermissionsActivity"
    android:exported="false"
    android:label="MYRAA Permissions" />
